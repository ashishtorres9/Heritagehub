[build]
  publish = "dist/public"
  command = "npm run build:netlify"

# SPA fallback so client-side routing works on refresh
[[redirects]]
  from = "/api/*"
  to = "/index.html"
  status = 200

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

